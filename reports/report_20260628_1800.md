# Trade Review Report

Generated: 2026-06-28T18:00:28.922857+00:00

Data window: 2026-06-27T18:03:23.401485Z -> 2026-06-28T15:01:31.218491Z

---

### Post-Trade Attribution Review
**Window:** 2026-06-27T18:03:23Z – 2026-06-28T15:01:31Z

**1. Overall Summary**
For the past 24 hours, the system recorded **0.0 PnL** with an undefined profit factor and win rate, operating in a purely passive execution state with zero completed round-trips.

**2. Per-Leg (Symbol) Performance**
Data unavailable. The `by_symbol` and `exit_by_reason` objects are null. No positions were closed during this window, resulting in no per-leg attribution metrics to evaluate.

**3. Risk-Gate (Reject) Status**
The system processed 16 entry orders, of which 8 were rejected (a 50% reject rate). All rejects fell under an `"other"` classification. 
* *Net-Dir / Allocator Status:* The current JSON payload lacks specific `net-dir` or `allocator` text tags. The presence of 8 unclassified rejects strongly implies that either the Net-Dir zeroing logic or the USDJPY allocator block is actively intercepting orders. **Action required:** Update the logging schema to explicitly map `net_dir_breach` and `usdjpy_blocked` to distinct reject classes for precise attribution.

**4. Hard-Stop Trigger**
* **Triggers:** 0
* **Meaning:** The hard_stop represents the ultimate cash-based equity drawdown limit. Even though it did not trigger today, it serves as the final systemic circuit breaker. A zero count confirms that no catastrophic intra-session equity drops occurred, keeping the system safely within its maximum tolerable loss boundaries.

**5. Leverage Usage**
Utilization was highly controlled and compliant. Leverage fluctuated narrowly between **0.6x and 0.9x**, averaging **0.8x**. This confirms the strategy is operating well under standard margin limits without stressing capital.

**6. Actionable Suggestions for Human Review**
In strict accordance with the "guardrails over alpha" philosophy (where AI provides systemic checks rather than numerical alpha generation), please review the following parameter updates:

1. **Fix Reject Telemetry:** Investigate the 8 orders in the `"other"` reject bucket. Modify the order router logic so that explicit flags (e.g., `NET_DIR_ZEROED`, `ALLOCATOR_USDJPY_BLOCK`) are passed to the post-trade database, rather than defaulting to `"other"`.
2. **Review Entry Signal Quality:** With 16 entries attempted and 0 exits logged, verify if the human-generated entry signals are misaligned with current market volatility, causing the risk gates to repeatedly block the AI from taking exposure.