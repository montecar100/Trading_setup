# Trade Review Report

Generated: 2026-06-28T00:00:27.695201+00:00

Data window: 2026-06-27T00:01:15.205938Z -> 2026-06-27T21:01:11.185538Z

---

### 24-Hour Post-Trade Review: 2026-06-27

**1. Overall Summary**
The trading system generated **$0.00 PnL** over the past 24 hours with a **0.0% win rate** and an **undefined profit factor**. All 16 attempted entries were systematically blocked prior to execution, resulting in a fully flat trading day. 

**2. Per-Leg (Symbol) Performance Comparison**
No symbol performance is available (`by_symbol` is empty). Because all 16 entries were intercepted by pre-trade logic, no positions were established or closed.

**3. Risk-Gate (Reject) Status**
Risk guardrails were highly active, blocking 100% of trade attempts (16 entries vs. 8 discrete reject clusters). 
* **Net-Dir & Allocator Status:** The JSON payload classifies all 8 rejections under a generic `"other"` category. Consequently, it cannot be definitively confirmed from this data alone if the `net-dir` zeroing logic or the `USDJPY` allocator blocks were the specific triggers. 
* **Action Required:** System logging needs to be updated to explicitly tag `net-dir` and `allocator` rejects so they no longer fall into the ambiguous `"other"` bucket. 

**4. Hard Stop (Cash-Based Stop Loss) Status**
* **Triggers:** 0
* **Meaning:** The `hard_stop` represents an extreme, account-level guardrail designed to forcibly liquidate positions if unrealized losses threaten a predefined cash floor. A count of 0 is expected today, as the pre-trade risk gates successfully prevented any exposure from reaching the portfolio management layer.

**5. Leverage Usage**
* **Min:** 0.6 | **Max:** 0.9 | **Avg:** 0.8
* **Analysis:** The system safely operated well within standard limits, maintaining conservative exposure and never approaching 1.0x leverage. 

**6. Actionable Parameter-Tuning Suggestions**
In alignment with the "guardrails over alpha" philosophy, the following parameters are flagged for **manual human review and implementation**—they will not be auto-executed:
1. **Fix Telemetry Classification:** A human operator must investigate the system logs to map the 8 `"other"` rejects to their exact root causes. If `net-dir` zeroing or the `USDJPY` allocator are triggering false positives, the thresholds governing those specific risk gates require manual recalibration to allow healthy flow.
2. **Review Pre-Trade Thresholds:** With 16 consecutive entry attempts blocked over 24 hours, human oversight is required to determine if the strategy's entry parameters have decoupled from current market conditions, or if the pre-trade risk gates are configured too tightly to permit any trades at all.